/**
 * Lenis 平滑滾動時長（秒）。
 */
export const lenisDurationSeconds = 1.15

/**
 * Lenis 輪軸靈敏度倍率。
 */
export const lenisWheelMultiplier = 0.85

/**
 * 單次場景轉場動畫時長（秒）。
 */
export const sceneTransitionDurationSeconds = 1.45

/**
 * 滾輪觸發場景切換的最小 delta 門檻。
 */
export const sceneWheelThreshold = 18

/**
 * 觸控滑動觸發場景切換的最小距離（px）。
 */
export const sceneTouchSwipeThresholdPx = 48

/**
 * 轉場結束後的輸入冷卻（毫秒），避免觸控板慣性連切兩幕。
 */
export const sceneInputCooldownMs = 140

/**
 * 3D 場景透視距離（px）。
 */
export const depthPerspectivePx = 1400

/**
 * 焦點可見距離（px）；超過後場景逐漸淡出。
 */
export const depthFocusDistancePx = 980
